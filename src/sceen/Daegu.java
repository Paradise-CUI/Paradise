package sceen;

// 대구 도시의 씬 생성

import setting.Inventory;
import setting.Player;

public class Daegu {
    public static void daegu(Player player, Inventory inventory) {
        FightSceen.fightSceen(6, player, inventory);
    }
}
