package sceen;

// 서울 도시의 씬 생성

import setting.Inventory;
import setting.Player;

public class Seoul {
    public static void seoul(Player player, Inventory inventory) {
        FightSceen.fightSceen(1, player, inventory);
    }
}
